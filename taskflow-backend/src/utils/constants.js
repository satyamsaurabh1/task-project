const USER_ROLES = {
    ADMIN: 'admin',
    USER: 'user'
};

const TASK_STATUSES = ['pending', 'in-progress', 'completed'];
const TASK_PRIORITIES = ['low', 'medium', 'high'];

module.exports = {
    USER_ROLES,
    TASK_STATUSES,
    TASK_PRIORITIES
};
